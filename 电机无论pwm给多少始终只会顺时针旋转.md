\# 问题：电机反转失效，底层驱动GPIO端口配置错误



\## 背景

\- \*\*项目\*\*：倒立摆角度环调试

\- \*\*主控\*\*：STM32F103C8T6

\- \*\*电机驱动\*\*：TB6612 (方向引脚 PB12, PB13；PWM引脚 PA1)

\- \*\*关键函数\*\*：`Motor\_SetPWM()`



\## 核心结论

\*\*`Motor\_SetPWM` 函数中，处理反向旋转的 `else` 分支错误地操作了 `GPIOA` 的引脚，导致方向控制信号未送达驱动板，电机只能单向旋转。\*\*



\## 现象描述

1\.  发送正转指令（`Motor\_SetPWM(30)`）：电机顺时针旋转，正常。

2\.  发送反转指令（`Motor\_SetPWM(-30)`）：电机\*\*依然顺时针旋转\*\*，无法反向。

3\.  后果：倒立摆角度环在需要反向修正时，系统反而加速偏离，立刻失控。



\## 故障树排查

1\.  \*\*假设：PID控制极性反了\*\*

&#x20;   -   \*\*验证\*\*：临时将输出改为 `Motor\_SetPWM(-Out)`。

&#x20;   -   \*\*结果\*\*：现象不仅没解决，反而更混乱。

&#x20;   -   \*\*结论\*\*：排除了控制算法层的极性错误。

2\.  \*\*假设：PID计算频率有问题\*\*

&#x20;   -   \*\*验证\*\*：将 `Motor\_SetPWM` 从条件判断 `if(count>=40)` 中移出，确保每个PWM周期都执行。

&#x20;   -   \*\*结果\*\*：问题依旧。

&#x20;   -   \*\*结论\*\*：排除了调用频率导致的信号中断。

3\.  \*\*假设：底层驱动本身无法反转\*\*

&#x20;   -   \*\*验证\*\*：编写开环测试程序，直接给固定值 `Motor\_SetPWM(30)` 和 `Motor\_SetPWM(-30)`。

&#x20;   -   \*\*结果\*\*：正向正常，反向仍然正转。

&#x20;   -   \*\*结论\*\*：\*\*问题被精确定位在 `Motor\_SetPWM` 驱动函数内部。\*\*

4\.  \*\*最终定位\*\*

&#x20;   -   逐行审查 `Motor\_SetPWM` 函数代码，发现在处理负值的 `else` 分支中，方向引脚操作的端口是 `GPIOA`，而初始化时配置的是 `GPIOB`。

&#x20;   -   \*\*根因\*\*：此段代码从旧工程复制而来，因旧工程硬件接线不同，代码被临时修改但未在移植后恢复。



\## 关键代码修改

```diff

&#x20;void Motor\_SetPWM(int16\_t Speed)

&#x20;{

&#x20;    if (Speed >= 0) {

&#x20;        GPIO\_ResetBits(GPIOB, GPIO\_Pin\_12);

&#x20;        GPIO\_SetBits(GPIOB, GPIO\_Pin\_13);

&#x20;    } else {

\-        GPIO\_SetBits(GPIOA, GPIO\_Pin\_12);    // 错误：应为GPIOB

\-        GPIO\_ResetBits(GPIOA, GPIO\_Pin\_13);  // 错误：应为GPIOB

\+        GPIO\_SetBits(GPIOB, GPIO\_Pin\_12);

\+        GPIO\_ResetBits(GPIOB, GPIO\_Pin\_13);

&#x20;    }

&#x20;}

